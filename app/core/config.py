import os

# Define the base directory (project root)
BASE_DIR = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))

# Define the data directory path (app/data)
DATA_DIR = os.path.join(BASE_DIR, "data")
